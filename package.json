{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@typescript-eslint/eslint-plugin": "^7.1.1",
    "@typescript-eslint/parser": "^7.1.1",
    "eslint": "^8.57.0"
  },
  "devDependencies": {
    "lerna": "^8.1.2"
  },
  "scripts": {
    "dev:backend": "lerna run dev --scope=backend",
    "start:backend": "lerna run build --scope=backend && lerna run start --scope=backend"
  }
}
