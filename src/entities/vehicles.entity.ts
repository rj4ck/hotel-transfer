export interface IVehicleImages {
    url: string;
    type: string;
}

export interface IVehicles {
    code: string;
    name: string;
}

export interface ICategories {
    code: string;
    name: string;
}
